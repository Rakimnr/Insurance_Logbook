name: insurance_logbook
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # UI / Icons
  cupertino_icons: ^1.0.8

  # Local storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5

  # State management (we’ll use this later for proper MVVM)
  flutter_riverpod: ^3.0.3

  # 🔹 New: external actions (WhatsApp / browser links, etc.)
  url_launcher: ^6.3.0

  file_picker: ^8.1.2
  open_filex: ^4.5.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
